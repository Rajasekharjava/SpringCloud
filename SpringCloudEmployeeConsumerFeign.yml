server:
  port: 2027
  
spring:
  application:
    name: SpringCloudEmployeeConsumerFeign
    
eureka:
  instance:
    hostname: localhost
  client: 
    register-with-eureka: true
    fetch-registry: true
    serviceUrl:
defaultZone: http://localhost:2022/eureka/
