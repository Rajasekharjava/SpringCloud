server:
  port: 2024
  
spring:
  application:
    name: SpringCloudEmployeeConsumer
    
eureka:
  instance:
    hostname: localhost
  client: 
    register-with-eureka: true
    fetch-registry: true
    serviceUrl:
defaultZone: http://localhost:2022/eureka/
