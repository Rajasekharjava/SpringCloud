
server:
  port: 2026
  
spring:
  application:
    name: SpringCloudEmployeeConsumerRibbon
    
eureka:
  instance:
    hostname: localhost
  client: 
    register-with-eureka: true
    fetch-registry: true
    serviceUrl:
defaultZone: http://localhost:2022/eureka/
